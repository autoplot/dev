This is a demo of how .vap files can be parameterized.  Within the vap file, 
the parameter %{PWD} has been added.  This is then resolved when the .vap
is loaded:

autoplotSiteStats.vap?APP=autoplot

