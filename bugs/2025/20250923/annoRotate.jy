ann0= annotation(1,text='This annotation should not jump as it spins',
   anchorPosition='NW')

ann= annotation(text='Hello Jeremy Faden',anchorOffset='',borderType='rectangle')

for i in xrange(40):
    for a in xrange(0,360,1):
        ann.rotate= a
        sleep(10)
        if ( a%90==0 ): 
            dom.canvases[0].controller.dasCanvas.repaint()
            sleep(400)
